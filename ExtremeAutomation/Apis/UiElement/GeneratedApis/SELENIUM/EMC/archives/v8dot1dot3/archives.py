from ExtremeAutomation.Apis.UiElement.GeneratedApis.SELENIUM.EMC.archives.v8dot1dot2.archives import Archives as \
    ArchivesBase
# All imports above this line will be removed after generation.
# User imports must be added below.


class Archives(ArchivesBase):
    def archives_confirm_details_archive_run_governance(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Archives, self).archives_confirm_details_archive_run_governance(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def archives_confirm_details_archive_regime(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Archives, self).archives_confirm_details_archive_regime(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def archives_confirm_details_version_different_count(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Archives, self).archives_confirm_details_version_different_count(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def archives_confirm_details_version_description(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Archives, self).archives_confirm_details_version_description(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def archives_confirm_details_version_description_contains(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Archives, self).archives_confirm_details_version_description_contains(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()
