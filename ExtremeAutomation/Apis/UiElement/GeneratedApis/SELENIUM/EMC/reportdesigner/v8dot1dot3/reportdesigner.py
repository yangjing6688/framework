from ExtremeAutomation.Library.Device.Common.Apis.UiBaseApi import UiBaseApi as ReportdesignerBase
# All imports above this line will be removed after generation.
# User imports must be added below.


class Reportdesigner(ReportdesignerBase):
    def reportdesigner_collapse_tree_panel(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_collapse_tree_panel(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_expand_tree_panel(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_expand_tree_panel(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_select_tree_panel(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_select_tree_panel(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_myreports_collapse_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_myreports_collapse_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_myreports_expand_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_myreports_expand_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_myreports_select_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_myreports_select_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_systemreports_collapse_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_systemreports_collapse_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_systemreports_expand_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_systemreports_expand_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_systemreports_select_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_systemreports_select_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_mycomponents_collapse_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_mycomponents_collapse_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_mycomponents_expand_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_mycomponents_expand_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_mycomponents_select_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_mycomponents_select_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_components_collapse_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_components_collapse_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_components_expand_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_components_expand_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_components_select_tree_node(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_components_select_tree_node(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_click_new(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_click_new(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_click_edit(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_click_edit(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_click_save(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_click_save(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_click_delete(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_click_delete(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_new_set_report_name(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_new_set_report_name(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_new_set_category(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_new_set_category(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_new_set_layout(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_new_set_layout(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_new_click_custom(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_new_click_custom(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_new_custom_set_rows(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_new_custom_set_rows(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_new_custom_set_columns(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_new_custom_set_columns(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_new_set_min_height(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_new_set_min_height(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_new_set_include_toolbar(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_new_set_include_toolbar(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_new_click_ok(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_new_click_ok(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_new_click_cancel(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_new_click_cancel(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dialog_confirm_delete(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dialog_confirm_delete(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def reportdesigner_dragdrop_component(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Reportdesigner, self).reportdesigner_dragdrop_component(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()
