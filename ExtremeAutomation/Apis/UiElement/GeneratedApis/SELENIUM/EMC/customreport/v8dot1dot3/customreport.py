from ExtremeAutomation.Library.Device.Common.Apis.UiBaseApi import UiBaseApi as CustomreportBase
# All imports above this line will be removed after generation.
# User imports must be added below.


class Customreport(CustomreportBase):
    def customreport_set_category(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_set_category(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_set_time_period(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_set_time_period(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_set_target_type(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_set_target_type(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_set_target_value(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_set_target_value(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_set_statistic(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_set_statistic(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_expand_display_options(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_expand_display_options(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_collapse_display_options(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_collapse_display_options(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_display_set_title(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_display_set_title(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_display_set_render_as(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_display_set_render_as(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_display_set_chart_type(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_display_set_chart_type(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_display_set_x_axis_title(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_display_set_x_axis_title(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_display_set_y_axis_title(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_display_set_y_axis_title(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_click_save_to_report_designer(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_click_save_to_report_designer(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_click_export_to_csv(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_click_export_to_csv(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_click_bookmark(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_click_bookmark(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()

    def customreport_click_submit(self, ui_cmd_obj, arg_dict):
        # This try/except is here to protect any inheritance that might be used.
        # If a super class is specified the super class's function will be called.
        # If no super class is specified the ui_cmd_obj is returned.
        # This can all be removed if a user implements the function.
        try:
            super(Customreport, self).customreport_click_submit(ui_cmd_obj, arg_dict)
        except AttributeError:
            return self.base_function()
